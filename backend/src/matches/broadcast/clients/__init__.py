"""Broadcaster implementations.

This is the home for pluggable clients — every one subclasses
``src.matches.broadcast.base.Broadcaster``. Only the no-op ships; see the
package ``README.md`` for how to add your own (AppSync, Pusher, WebSocket…).
"""
